doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario 
            h1= nombrePagina 
            p.reset Agrega tu e-mail para reestablecer la contraseña

            if locals.mensajes 
                .alertas 
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form.caja-login(method="POST" action="/reestablecer")
                .campo.form-group 
                    label(for="email") Email: 
                    input(type="email"  name="email" placeholder="Tu Email" class="form-control")
                .campo.enviar 
                    input(type="submit" value="Reestablecer Password" class="boton")
                .campo.acciones 
                    a(href="/iniciar-sesion") Ya lo recordé, volver al login