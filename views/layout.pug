doctype html
html(lang="en")
    head
        include partials/header
    body 

        header
            nav.navbar.navbar-expand-lg.navbar-light.bg-light.justify-content-between
                a.orbitron.navbar-brand(href='/') TaskMan
                a.btn.btn-outline-success.my-2.my-sm-0(href="/cerrar-sesion") Cerrar Sesión

        main.contenedor
            aside.contenedor-proyectos 
                .panel.crear-proyecto 
                    a(href="/nuevo-proyecto" class="boton") Nuevo Proyecto
                        span +
                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos 
                        if(proyectos.length)
                            each proyecto in proyectos
                                li 
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre 


            block contenido

            //- pre= vardump(proyectos)

        script(src="/dist/bundle.js")    
